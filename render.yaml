services:
  - type: web
    name: the-daily-dose-api
    env: docker
    plan: starter
    autoDeploy: true
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health
    domains:
      - api.360-encompass.com
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: the-daily-dose-db
          property: connectionString
      - key: AUTO_CREATE_TABLES
        value: "false"
      - key: CORS_ALLOWED_ORIGINS
        value: "https://360-encompass.com,https://www.360-encompass.com,http://localhost:3000,http://127.0.0.1:3000"
      - key: JWT_SECRET
        sync: false
      - key: AWS_REGION
        value: us-east-1
      - key: S3_BUCKET
        value: vehr-uploads-prod
      - key: AWS_ACCESS_KEY_ID
        sync: false
      - key: AWS_SECRET_ACCESS_KEY
        sync: false
      - key: S3_ENDPOINT_URL
        sync: false
      - key: S3_USE_PATH_STYLE
        sync: false
      - key: S3_PRESIGN_EXPIRES_SECONDS
        sync: false
      - key: S3_VALIDATE_ON_STARTUP
        value: "true"
      - key: INTEGRATION_TOKEN_KEY
        sync: false
      - key: RINGCENTRAL_CLIENT_ID
        value: 5XvBxFN98oMfWMhTC0wn0s
      - key: RINGCENTRAL_CLIENT_SECRET
        sync: false
      - key: RINGCENTRAL_SERVER_URL
        value: https://platform.ringcentral.com
      - key: RINGCENTRAL_REDIRECT_URI
        value: https://api.360-encompass.com/api/v1/integrations/ringcentral/callback
      - key: RINGCENTRAL_WEBHOOK_SECRET
        sync: false
      - key: RINGCENTRAL_POST_CONNECT_REDIRECT
        value: https://360-encompass.com/admin-center

  - type: web
    name: the-trapp-house-web
    env: node
    plan: starter
    autoDeploy: true
    rootDir: frontend
    buildCommand: npm ci && npm run build
    startCommand: npx next start -p $PORT
    healthCheckPath: /
    domains:
      - 360-encompass.com
      - www.360-encompass.com
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: NEXT_PUBLIC_API_BASE_URL
        value: https://api.360-encompass.com
      - key: NEXT_PUBLIC_API_TOKEN
        sync: false

databases:
  - name: the-daily-dose-db
    plan: starter
    databaseName: the_daily_dose
    user: the_daily_dose_user
